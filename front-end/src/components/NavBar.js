import { Link } from 'react-router-dom'

const NavBar = () => {
    <nav>
        <Link to="/travelPackages">Trek Star *</Link>
        <Link to="/travelPackages/new">Create new Package</Link>
    </nav>
}

export default NavBar;